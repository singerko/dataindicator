/**
 * Základný unit test pre overenie testovacej infraštruktúry.
 */
package sk.dataindicator

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Ukážkový test pre začiatok.
 */
class SimpleUnitTest {
    /**
     * Jednoduchý test, ktorý overí základnú aritmetiku.
     */
    @Test
    fun basicMath_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
